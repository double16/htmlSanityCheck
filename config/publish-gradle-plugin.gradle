
pluginBundle {
    website = 'https://github.com/double16/htmlSanityCheck'
    vcsUrl = 'https://github.com/double16/htmlSanityCheck'
    description = 'Checks HTML files for broken links, missing images etc.'
    tags = ['checks', 'quality', 'documentation', 'html', 'asciidoctor', 'test']

    plugins {
        htmlSanityCheck {
            id = 'com.patdouble.htmlSanityCheck'
            displayName = 'htmlSanityCheck plugin'

        }
    }
}
